# ──────────────────────────────────────────────────────────────────────────────
# vLLM NVFP4+MTP — Qwen3.6-27B (Huihui abliterated) on RTX 5090
# ──────────────────────────────────────────────────────────────────────────────
#
# Engine:    vLLM v0.23.0
# Model:     Qwen3.6-27B NVFP4 (sakamakismile — Huihui abliterated + MTP)
# Drafter:   MTP n=3 (embedded MTP head, bf16)
# KV cache:  fp8_e4m3 (Blackwell-native)
# Vision:    yes (mmproj, Qwen3.5 arch)
# Context:   209K tokens (~214K safe ceiling)
# Concurrency: 4 sequences
# Perf mode: interactivity
#
# Weights:
#   https://huggingface.co/sakamakismile/Huihui-Qwen3.6-27B-abliterated-NVFP4-MTP
#   ~19 GB, expects: $MODEL_DIR/huihui-qwen3.6-27b-abliterated-nvfp4-mtp/model.safetensors
#
# Env overrides:
#   MODEL_DIR         weights root            (default: ../models)
#   MODEL_ALIAS       served model name       (default: local)
#   MAX_MODEL_LEN     context length          (default: 209200)
#   GPU_MEMORY_UTIL   VRAM fraction           (default: 0.94)
#   SPEC_N_MAX        MTP draft tokens        (default: 3)
#   MAX_NUM_SEQS      max concurrent          (default: 4)
#   PORT              host port               (default: 8020)
#   VLLM_IMAGE        Docker image            (default: vllm/vllm-openai:v0.23.0)
#
# ──────────────────────────────────────────────────────────────────────────────

services:
  vllm-nvfp4-vision-mtp:
    image: ${VLLM_IMAGE:-vllm/vllm-openai:v0.23.0}
    container_name: "vllm-nvfp4-vision-mtp"
    restart: unless-stopped
    ports:
      - "${PORT:-8020}:8000"
    volumes:
      - ${MODEL_DIR:-../models}:/root/.cache/huggingface
      - ../cache/torch_compile:/root/.cache/vllm/torch_compile_cache
      - ../cache/triton:/root/.triton/cache
      - ../cache/flashinfer:/root/.cache/flashinfer
    environment:
      - NVIDIA_VISIBLE_DEVICES=${GPU_DEVICE:-0}
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
      - VLLM_WORKER_MULTIPROC_METHOD=spawn
      - VLLM_BUILD_COMMIT=
      - VLLM_BUILD_PIPELINE=
      - VLLM_BUILD_URL=
      - VLLM_IMAGE_TAG=
      - VLLM_NO_USAGE_STATS=1
      - OMP_NUM_THREADS=1
      - PYTORCH_CUDA_ALLOC_CONF=${PYTORCH_CUDA_ALLOC_CONF:-expandable_segments:True,max_split_size_mb:512}
      - VLLM_ALLOW_LONG_MAX_MODEL_LEN=1
      - VLLM_FLOAT32_MATMUL_PRECISION=high
      - VLLM_USE_FLASHINFER_SAMPLER=1
      - TRITON_CACHE_DIR=/root/.triton/cache
      - CUDA_DEVICE_MAX_CONNECTIONS=8
    shm_size: "16gb"
    ipc: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["${GPU_DEVICE:-0}"]
              capabilities: [gpu]
    command:
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --model
      - /root/.cache/huggingface/huihui-qwen3.6-27b-abliterated-nvfp4-mtp
      - --served-model-name
      - ${MODEL_ALIAS:-local}
      - --quantization
      - compressed-tensors
      - --dtype
      - bfloat16
      - --tensor-parallel-size
      - "1"
      - --max-model-len
      - "${MAX_MODEL_LEN:-209200}"
      - --gpu-memory-utilization
      - "${GPU_MEMORY_UTIL:-0.94}"
      - --max-num-seqs
      - "${MAX_NUM_SEQS:-4}"
      - --max-num-batched-tokens
      - "${MAX_NUM_BATCHED:-4096}"
      - --kv-cache-dtype
      - fp8_e4m3
      - --trust-remote-code
      - --reasoning-parser
      - qwen3
      - --enable-auto-tool-choice
      - --tool-call-parser
      - qwen3_coder
      - --enable-prefix-caching
      - --enable-chunked-prefill
      - --speculative-config
      - '{"method":"mtp","num_speculative_tokens":${SPEC_N_MAX:-3}}'
      - --performance-mode
      - interactivity
